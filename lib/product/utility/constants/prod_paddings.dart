import 'package:flutter/material.dart';

/// Product`s padding class
final class ProductPaddings {
  const ProductPaddings._();

  /// Large padding is 32px
  static const double padding32 = 32;

  /// All large padding is 32px
  static const EdgeInsets allLarge = EdgeInsets.all(padding32);

  /// Horizontal large padding is 32px
  static const EdgeInsets horizontalLarge = EdgeInsets.symmetric(
    horizontal: padding32,
  );

  /// Vertical large padding is 32px
  static const EdgeInsets verticalLarge = EdgeInsets.symmetric(
    vertical: padding32,
  );

  /// Top large padding is 32px
  static const EdgeInsets topLarge = EdgeInsets.only(top: padding32);

  /// Bottom large padding is 32px
  static const EdgeInsets bottomLarge = EdgeInsets.only(bottom: padding32);

  /// Left large padding is 32px
  static const EdgeInsets leftLarge = EdgeInsets.only(left: padding32);

  /// Right large padding is 32px
  static const EdgeInsets rightLarge = EdgeInsets.only(right: padding32);

  /// Medium padding is 24px
  static const double padding24 = 24;

  /// All medium padding is 24px
  static const EdgeInsets allMedium = EdgeInsets.all(padding24);

  /// Horizontal medium padding is 24px
  static const EdgeInsets horizontalMedium = EdgeInsets.symmetric(
    horizontal: padding24,
  );

  /// Vertical medium padding is 24px
  static const EdgeInsets verticalMedium = EdgeInsets.symmetric(
    vertical: padding24,
  );

  /// Top medium padding is 24px
  static const EdgeInsets topMedium = EdgeInsets.only(top: padding24);

  /// Bottom medium padding is 24px
  static const EdgeInsets bottomMedium = EdgeInsets.only(bottom: padding24);

  /// Left medium padding is 24px
  static const EdgeInsets leftMedium = EdgeInsets.only(left: padding24);

  /// Right medium padding is 24px
  static const EdgeInsets rightMedium = EdgeInsets.only(right: padding24);

  /// SmallMedium padding is 16px
  static const double padding16 = 16;

  /// All small medium padding is 16px
  static const EdgeInsets allSmallMedium = EdgeInsets.all(padding16);

  /// Horizontal small medium padding is 16px
  static const EdgeInsets horizontalSmallMedium = EdgeInsets.symmetric(
    horizontal: padding16,
  );

  /// Vertical small medium padding is 16px
  static const EdgeInsets verticalSmallMedium = EdgeInsets.symmetric(
    vertical: padding16,
  );

  /// Top small medium padding is 16px
  static const EdgeInsets topSmallMedium = EdgeInsets.only(top: padding16);

  /// Bottom small medium padding is 16px
  static const EdgeInsets bottomSmallMedium = EdgeInsets.only(
    bottom: padding16,
  );

  /// Left small medium padding is 16px
  static const EdgeInsets leftSmallMedium = EdgeInsets.only(left: padding16);

  /// Right small medium padding is 16px
  static const EdgeInsets rightSmallMedium = EdgeInsets.only(right: padding16);

  /// ExtraSmall padding is 8px
  static const double padding8 = 8;

  /// All extra small padding is 8px
  static const EdgeInsets allExtraSmall = EdgeInsets.all(padding8);

  /// Horizontal extra small padding is 8px
  static const EdgeInsets horizontalExtraSmall = EdgeInsets.symmetric(
    horizontal: padding8,
  );

  /// Vertical extra small padding is 8px
  static const EdgeInsets verticalExtraSmall = EdgeInsets.symmetric(
    vertical: padding8,
  );

  /// Top extra small padding is 8px
  static const EdgeInsets topExtraSmall = EdgeInsets.only(top: padding8);

  /// Bottom extra small padding is 8px
  static const EdgeInsets bottomExtraSmall = EdgeInsets.only(bottom: padding8);

  /// Left extra small padding is 8px
  static const EdgeInsets leftExtraSmall = EdgeInsets.only(left: padding8);

  /// Right extra small padding is 8px
  static const EdgeInsets rightExtraSmall = EdgeInsets.only(right: padding8);
}
