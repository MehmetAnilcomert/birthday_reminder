/// Authentication status enum.
enum AuthStatus {
  /// Initial state.
  initial,

  /// Loading state.
  loading,

  /// Authenticated state.
  authenticated,

  /// Unauthenticated state.
  unauthenticated,

  /// Error state.
  error,
}
